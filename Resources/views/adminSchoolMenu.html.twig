{% extends "ClarolineCoreBundle:Administration:layout.html.twig" %}

{% block breadcrumb %}
    {{
    macros.breadcrumbs([
    {
    'icon': 'fa fa-cog',
    'name': 'administration'|trans({}, 'platform'),
    'href': path('claro_admin_index')
    },
    {
    'name': 'Gestion école',
    'href': ''
    }
    ])
    }}

{% endblock %}

{% block section_content %}
    <div class="panel-heading">
        <h3 class="panel-title">Gestion école</h3>
    </div>
    <div class="list-group">
        <a class="list-group-item" href="{{ path('laurentAdminSchoolImportClasses') }}" id="link_Import_classe">
            Importer les classes
        </a>

        <a class="list-group-item" href="{{ path('laurentAdminSchoolCS') }}" id="link_Import_eleves">
            Importer les élèves [CS]
        </a>

        <a class="list-group-item" href="{{ path('laurentAdminSchoolCS') }}" id="link_Import_profs">
            Importer les profs [CS]
        </a>

        <a class="list-group-item" href="{{ path('laurentAdminSchoolCS') }}" id="link_Import_horaires">
            Importer les horaires [CS]
        </a>


    </div>
{% endblock %}