{% extends "ClarolineCoreBundle:Administration:layout.html.twig" %}

{% block breadcrumb %}
    {{
    macros.breadcrumbs([
    {
    'icon': 'fa fa-cog',
    'name': 'administration'|trans({}, 'platform'),
    'href': path('claro_admin_index')
    },
    {
    'name': 'Gestion école',
    'href': ''
    }
    ])
    }}

{% endblock %}

{% block section_content %}
    <div class="panel-heading">
        <h3 class="panel-title">Gestion école</h3>
    </div>
    <div class="list-group">
        <a class="list-group-item" href="{{ path('laurentAdminSchoolImportClasses') }}" id="link_Import_classe">
            Importer les classes [Dev]
        </a>

        <a class="list-group-item" href="{{ path('laurentAdminSchoolImportElevesInClasses') }}" id="link_Import_eleves_in_classe">
            Ajouter les élèves dans leur classe [Dev]
        </a>

        <a class="list-group-item" href="{{ path('laurentAdminSchoolCS') }}" id="link_Import_eleves">
            Importer les élèves [CS]
        </a>

        <a class="list-group-item" href="{{ path('laurentAdminSchoolCS') }}" id="link_Import_profs">
            Importer les profs [CS]
        </a>

        <a class="list-group-item" href="{{ path('laurentAdminSchoolCS') }}" id="link_Import_horaires">
            Importer les horaires [COD]
        </a>

        <a class="list-group-item" href="{{ path('laurentAdminSchoolImportMatieres') }}" id="link_Import_matiere">
            Importer les matières [Dev]
        </a>

        <a class="list-group-item" href="{{ path('laurentAdminSchoolImportProfInMatiere') }}" id="link_Import_profInMatiere">
            Lier les matières aux profs [Dev]
        </a>


    </div>
{% endblock %}
